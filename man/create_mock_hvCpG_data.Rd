% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_mock.R
\name{create_mock_hvCpG_data}
\alias{create_mock_hvCpG_data}
\title{Create a mock hvCpG dataset with realistic lambda and SD values}
\usage{
create_mock_hvCpG_data(
  outdir = system.file("mock_data", package = "hyperVarMeth"),
  n_datasets = 10,
  n_samples_per_dataset = 3,
  n_cpgs = 200,
  seed = 1234,
  lowvar_dataset = "DS1",
  lowvar_multiplier = 0.25
)
}
\arguments{
\item{outdir}{Directory for output files (default "mock_data").}

\item{n_datasets}{Number of mock datasets (default 3).}

\item{n_samples_per_dataset}{Samples per dataset (default 4).}

\item{n_cpgs}{Number of CpGs (default 200).}

\item{seed}{Random seed (default 1234).}

\item{lowvar_dataset}{Name of dataset with reduced variability (default "DS1").}

\item{lowvar_multiplier}{SD scaling factor for that dataset}
}
\value{
Invisibly, a list of paths and simulated CpG names.
}
\description{
Simulates a small HDF5 methylation matrix and per-dataset statistics
that mimic the logic used in the WGBS Atlas preprocessing pipeline:
\itemize{
\item simulate per-sample beta values with skew toward 0 and 1,
\item compute per-dataset median row SDs,
\item compute lambda = 95th percentile of row SDs / median SD.
}
}
