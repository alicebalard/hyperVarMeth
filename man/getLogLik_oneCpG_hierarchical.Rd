% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hvCpGDetection.R
\name{getLogLik_oneCpG_hierarchical}
\alias{getLogLik_oneCpG_hierarchical}
\title{Compute CpG-level hv marginal log-likelihood (hierarchical, 3-level)}
\usage{
getLogLik_oneCpG_hierarchical(
  Mdf,
  metadata,
  dataset_groups,
  ds_params,
  p0,
  p1,
  alpha,
  minind
)
}
\arguments{
\item{Mdf}{Numeric matrix (1 CpG x samples): methylation values for one CpG.}

\item{metadata}{Data frame with a \code{dataset} column (and \code{sample}).}

\item{dataset_groups}{Named list mapping dataset names to column indices of \code{Mdf}.}

\item{ds_params}{Data frame (rownames = dataset names) with columns \code{sd0}, \code{sd1}.}

\item{p0}{Numeric scalar: Pr(Z_k = 0 | Z = 0), the true-negative rate at the
dataset level (e.g. 0.95).}

\item{p1}{Numeric scalar: Pr(Z_k = 1 | Z = 1), the true-positive rate at the
dataset level (e.g. 0.65).}

\item{alpha}{Numeric scalar in (0, 1): prior probability that the CpG is hv
(the value being optimised).}

\item{minind}{Integer: minimum non-missing individuals a dataset must have to
contribute (datasets below this are skipped).}
}
\value{
Numeric scalar: the marginal log-likelihood \eqn{\log P(M)} for this
CpG at the given \code{alpha}. Returns \code{NA_real_} if no dataset met \code{minind}.
}
\description{
Evaluates the marginal log-likelihood of one CpG's methylation data under a
three-level hierarchical model, for a given value of \code{alpha}:
}
\details{
\itemize{
\item \strong{Z} (CpG level): the CpG is hypervariable (Z=1) with prior
probability \code{alpha}, or not (Z=0) with probability \code{1 - alpha}. There is a
single Z shared by all datasets.
\item \strong{Z_k} (dataset level): given Z, each dataset independently is in
its hv state with probability \code{p1} (if Z=1) or \code{1 - p0} (if Z=0). Datasets
are conditionally independent \emph{given Z}, so their evidence is
multiplied only inside each Z-branch.
\item \strong{M_ik} (individual level): given Z_k, each individual's value is
drawn from a Normal with SD \code{sd0} (stable) or \code{sd1 = lambda * sd0} (hv).
}

The CpG-level indicator Z is marginalised \strong{once}, at the top:
\deqn{P(M) = \alpha \prod_k [p_1 d^1_k + (1-p_1) d^0_k]
           + (1-\alpha) \prod_k [(1-p_0) d^1_k + p_0 d^0_k]}
where \eqn{d^1_k}, \eqn{d^0_k} are the products over individuals in dataset k of
the hv / stable Normal densities. Because \code{alpha} multiplies the whole product
over datasets (rather than entering per individual or per dataset), a large
dataset contributes a single confident dataset-level factor rather than one
vote per sample: this removes the sample-size (N) dominance of the older
summed-per-individual likelihood while keeping each dataset's full within-set
power. Note the per-CpG MLE of \code{alpha} is typically at the boundary (0 or 1);
informative in-between values come from estimating \code{alpha} across CpGs
(e.g. empirical Bayes), not from a single CpG.

Not used by the default pipeline (which reports scores via
\code{posterior_hv_1CpG}); provided for empirical-Bayes estimation of the global
prior \code{alpha0} by maximising the summed log-likelihood across CpGs.
}
