% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hvCpGDetection.R
\name{runOptim1CpG_gridrefine}
\alias{runOptim1CpG_gridrefine}
\title{Estimate the optimal alpha value for one CpG}
\usage{
runOptim1CpG_gridrefine(
  Mdf,
  metadata,
  dataset_groups,
  ds_params,
  p0,
  p1,
  minind
)
}
\arguments{
\item{Mdf}{Numeric matrix (1 CpG x samples). Methylation values for a single CpG.}

\item{metadata}{Data frame containing \code{sample} and \code{dataset} columns.}

\item{dataset_groups}{Named list mapping dataset names to row indices in \code{metadata}.}

\item{ds_params}{Data frame with precomputed parameters per dataset:
columns \code{sd0}, \code{sd1}, and rownames corresponding to dataset names.}

\item{p0, p1}{Numeric scalars: True negative and true positive rates.}

\item{minind}{Numeric scalar: Minimum number of individuals covered per dataset by CpG (default 3).}
}
\value{
Numeric scalar: estimated optimal alpha for the CpG.
}
\description{
Performs a coarse grid search followed by local refinement using
Brent optimization to find the alpha value (probability of hypervariability)
that maximises the log-likelihood for a single CpG.
}
