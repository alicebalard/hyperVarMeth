name: R-CMD-check

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  R-CMD-check:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        config:
          - {os: ubuntu-latest, r: '4.3'}
    name: R CMD check (${{ matrix.config.r }})
    steps:
      # Step 1: Checkout the repo
      - uses: actions/checkout@v3

      # Step 2: Set up R
      - name: Set up R
        uses: r-lib/actions/setup-r@v2
        with:
          r-version: ${{ matrix.config.r }}

      # Step 3: Install system dependencies
      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libcurl4-openssl-dev libssl-dev libxml2-dev qpdf

      # Step 4: Install R packages needed for building and testing
      - name: Install R package dependencies
        run: |
          Rscript -e "install.packages(c('remotes','testthat','roxygen2','devtools'))"
          Rscript -e "remotes::install_deps(dependencies = TRUE)"

      # Step 5: Run testthat tests (without devtools::test)
      - name: Run R CMD check
        run: Rscript -e "rcmdcheck::rcmdcheck(error_on = 'warning')"

      # Step 6: Run R CMD check
      - name: Run R CMD check
        run: R CMD check --no-manual --as-cran .
